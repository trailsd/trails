# ═══════════════════════════════════════════════════════════════
# TRAILS monorepo .gitignore
# Covers: Rust, Python, Go, Java, Kotlin, Swift, C, Docker, K8s
# ═══════════════════════════════════════════════════════════════

# ───────────────────────────────────────────────────────────────
# Rust  (server/, client-rust/, client-cli/)
# ───────────────────────────────────────────────────────────────
**/target/
debug/
**/*.rs.bk
*.pdb
**/mutants.out*/

# Cargo.lock policy:
#   Binary crates (server/, client-cli/) — TRACK the lock file.
#   Library crates (client-rust/)        — DO NOT track.
# The blanket ignore below covers libraries. Binaries override
# with negation rules further down.
Cargo.lock

# Override: track lock files for binary crates
!server/Cargo.lock
!client-cli/Cargo.lock

# ───────────────────────────────────────────────────────────────
# Python  (client-python/)
# ───────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
*.egg
dist/
build/
*.whl
.eggs/
.venv/
venv/
env/
ENV/
pip-log.txt
pip-delete-this-directory.txt
.pytest_cache/
.coverage
htmlcov/
.mypy_cache/
.ruff_cache/
.tox/
.nox/

# ───────────────────────────────────────────────────────────────
# Go  (client-go/)
# ───────────────────────────────────────────────────────────────
# Go binaries (named without extension on Unix)
# Covered by per-directory .gitignore if needed.
*.exe
*.exe~
*.dll
*.dylib
# Go test binary, coverage
*.test
*.out
*.prof
# Go workspace
go.work
go.work.sum

# ───────────────────────────────────────────────────────────────
# Java  (client-java/)
# ───────────────────────────────────────────────────────────────
*.class
*.jar
*.war
*.ear
*.nar
hs_err_pid*
replay_pid*

# Maven
**/target/
pom.xml.tag
pom.xml.releaseBackup
pom.xml.versionsBackup
pom.xml.next
release.properties
dependency-reduced-pom.xml
buildNumber.properties
.mvn/timing.properties
.mvn/wrapper/maven-wrapper.jar

# Gradle
.gradle/
!**/src/**/build/
gradle-app.setting
!gradle-wrapper.jar
!gradle-wrapper.properties

# ───────────────────────────────────────────────────────────────
# Kotlin / Android  (client-kotlin/)
# ───────────────────────────────────────────────────────────────
*.apk
*.aab
*.ap_
*.dex
local.properties
.cxx/
captures/
.externalNativeBuild/
.kotlin/

# ───────────────────────────────────────────────────────────────
# Swift / iOS  (client-swift/)
# ───────────────────────────────────────────────────────────────
.build/
Packages/
Package.resolved
xcuserdata/
*.xcscmblueprint
*.xccheckout
DerivedData/
*.moved-aside
*.pbxuser
!default.pbxuser
*.mode1v3
!default.mode1v3
*.mode2v3
!default.mode2v3
*.perspectivev3
!default.perspectivev3
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# ───────────────────────────────────────────────────────────────
# C  (client-c/)
# ───────────────────────────────────────────────────────────────
*.o
*.a
*.lib
*.lo
*.la
*.lai
*.slo
# CMake
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
CTestTestfile.cmake
_deps/
compile_commands.json
client-c/build/
client-c/out/

# ───────────────────────────────────────────────────────────────
# Kubernetes / Helm
# ───────────────────────────────────────────────────────────────
deploy/helm/trails/charts/
deploy/helm/trails/*.tgz

# ───────────────────────────────────────────────────────────────
# Postgres (local dev)
# ───────────────────────────────────────────────────────────────
pgdata/
*.sql.bak

# ───────────────────────────────────────────────────────────────
# IDE / Editor
# ───────────────────────────────────────────────────────────────
# VS Code — track shared settings, ignore personal
.vscode/
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json

# JetBrains (IntelliJ, CLion, GoLand, PyCharm, Android Studio)
.idea/
*.iml
*.iws
*.ipr
out/

# Vim / Neovim
*.swp
*.swo
*~
tags

# Emacs
\#*\#
.\#*
*.elc

# Sublime Text
*.sublime-project
*.sublime-workspace

# ───────────────────────────────────────────────────────────────
# OS artifacts
# ───────────────────────────────────────────────────────────────
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
ehthumbs.db
Desktop.ini

# ───────────────────────────────────────────────────────────────
# Secrets / credentials  (NEVER commit)
# ───────────────────────────────────────────────────────────────
*.pem
*.key
*.p12
*.pfx
*.jks
*.keystore
# Allow test fixtures
!**/testdata/*.pem
!**/testdata/*.key
.env
.env.*
!.env.example
secrets/